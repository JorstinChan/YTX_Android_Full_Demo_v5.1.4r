apply plugin: 'com.android.application'

android {
    /*signingConfigs {
        jorstinchan {
            keyAlias 'jorstinchan'
            keyPassword 'jorstinchan'
            storeFile file('E:/JorstinChan/Android_keystore/JorstinChan.keystore')
            storePassword 'jorstinchan'
        }
    }*/
    compileSdkVersion 16
    buildToolsVersion "20.0.0"
    defaultConfig {
        applicationId "com.yuntongxun.ecdemo"
        minSdkVersion 9
        versionCode 16
        versionName "5.1.4r"
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }

}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:20.0.0'
    compile files('libs/universal-image-loader-1.9.3.jar')
    compile files('libs/umeng-analytics-v5.4.2.jar')
    compile files('libs/pinyin4j-2.5.0.jar')
    compile files('libs/YTX_Android_Full_SDK_v5.1.4r.jar')
}
